func main()
  var i,z : int
  var x,y : float
  var c,d : char

  z = sum(34, z+1, 25*i/100, d/4);
  while i<z do
     i = z + sum(y+1, 22, x/2);
     y = z * sum(i+1, y*2, c, d);
  endwhile

  z = sum('a', d);
  z = sum(3, 12, k) * sum();
  c = sum(c, d, 'b');
endfunc
