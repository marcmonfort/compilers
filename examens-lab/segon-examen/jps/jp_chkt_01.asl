func main()
  var i,z : int
  var c,d : char

  z = sum(34, z+1);
  while 0.1 <= sum(z, sum(i*2, z+1), i+1) do
     c = 3*x - i;
  endwhile

  y = c + z;
  if z+3 then
     write sum(3.7, -5, z*z, 2.3-i);
     z = sum();
  endif 
endfunc
