func main()
  var i,z : int
  var a : array [10] of int
  var x : float
  var c,d : char

  z = max(34);
  z = max(z+1, 25*i/100, a[i-1]);
  x = max(a[i*2-1], x+1);
  z = max(10, i*z/2, a[0]+22);
  x = max(12, x, z);
  c = max('A', 'a', d);
  c = max('A', 'a', x+1, d);
  z = max(23, d, x+1);
endfunc
