func main()
  var i,j: int
  var a: array [10] of char
  var z: float

  for z in range(10, a[j]) do
      a[j] = f(c,i);
  endfor
endfunc

func f(a: array [10] of char, i: int): int
  var m: char
  var j,k: int

  for m in range(1, a, 2*j) do
     for q in range(i+1, 10) do
        if m>a[k+i] then m=a[i+1];
        else return a[m-1];
        endif
     endfor
  endfor
  return k;
endfunc

